{
  "name": "cra-templates",
  "version": "1.0.0",
  "description": "A collection of templates for Create React App",
  "author": "Vid Čufar",
  "repository": "https://github.com/cufarvid/cra-templates.git",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "husky:prepare": "husky install",
    "lerna:publish": "lerna publish",
    "format": "prettier --write \"**/*.{ts,tsx,html,scss,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,html,scss,json,md}\""
  },
  "lint-staged": {
    "*.{ts,tsx,html,scss,json,md}": "prettier --write"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lerna": "^7.2.0",
    "lint-staged": "^14.0.1",
    "prettier": "^3.0.3"
  }
}
